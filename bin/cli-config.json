{
  "help": "A small utility to generate text art using Figlet",
  "version": "0.0.1",
  "params": [
    {
      "name": "text",
      "help": "text to generate ascii art"
    }
  ],
  "options": [
    {
      "name": "font",
      "aliases": ["f"],
      "help": "font to use for drawing ascii art"
    },
    {
      "name": "list-fonts",
      "aliases": ["l"],
      "help": "lists available fonts"
    },
    {
      "name": "horizontal-layout",
      "aliases": ["hl"],
      "help": "value that indicates the horizontal layout to use",
      "acceptOnly": [
        "default",
        "full",
        "fitted",
        "controlled smushing",
        "universal smushing"
      ]
    },
    {
      "name": "vertical-layout",
      "aliases": ["vl"],
      "help": "value that indicates the vertical layout to use",
      "acceptOnly": [
        "default",
        "full",
        "fitted",
        "controlled smushing",
        "universal smushing"
      ]
    }
  ]
}
